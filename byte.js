/**
 * 8位的块，字节byte 内存单位
 * 而不是访问内存中单独的位
 * 字节：一个字节的取值范围为
 * 字节数组
 * 内存中的每个字节都是唯一的地址
 * 0000 0000
 * 1111 1111
 */

/**
 * js 字符串的长度与字节的长度
 */

{
  /**
   * 1个字节代表代表8比特
   * 表示一组8比特数据
   * 一个字节的取值范围在 0000, 0000 1111, 1111 之间
   *
   * 如果将一个数的补码作为 相对应的负数
   *
   *
   */

  const is255 = 0b11111111 === 255
}
